# Work resolver #3887 + #3888 — implemented, run, adjudicated (2026-08-11)

Session scope: the two resolver issues from the SHWEP audit, plus the related-holdings
feature Derek requested on top. Companion handoff (same day, different session):
`2026-08-11-shwep-reading-room-resolver-and-acquisitions.md`.

## Shipped

**#3888 (merged to main, PR #3889).** `bestEdition()` in `scripts/lib/holdings-resolver.mjs`
now ranks: cited edition (when held readable) → translated-completeness tier →
role/authority (critical ≥ princeps ≥ source-language ≥ manuscript ≥ translation, gated on
the work's composition language) → legacy tie-breaks. Backward compatible: no opts = exact
legacy order. Eval gate `scripts/eval/bestedition-shwep-eval.mjs` + committed 30-work
labeled fixture: legacy 19/30 → new 28/30. The classifier is a twin of
`src/app/shwep/works-cited.ts` — change both together.

**#3887 (on `worktree-feat+shwep-private-preview`).** New stages in
`scripts/enrichment/shwep-cited-works.mjs`, all folded into `--all` as standing passes:
- `--cluster-expand` — work_id siblings of confirmed editions re-confirmed as candidates.
  First run: 572 offered, 116 accepted across 43 works (incl. Turnèbe 1554 Greek editio
  princeps of the CH).
- `--work-id-audit` — read-only dupes + cross-assignments →
  `/tmp/shwep-cited/work-id-audit.{md,json}`. Containment via multi-work volumes is
  detected and suppressed (naive version was unreviewable: 92 "dupes" vs the real 6).
- `--related` — for cited works with NO readable copy (in-copyright/no-PD editions),
  ≤3 genuinely related holdings with fixed relation labels; editions of the work itself
  rejected. 96/107 works, 200 links → `src/data/shwep-related-works.ts` (`git add -f`).
  Rendered in the episode page "no copy matched" section.
- Extraction carries Primary/Secondary sectioning (`sec`) + `ed_cite` → `needs_review`
  gating (latent until next `--extract`).

**Adjudications:** merged Talmud→Babylonian Talmud, De dubitationibus→Damascius De
principiis, Sepher→Sefer ha-Razim (spelling dedupe miss — wid-audit can't see dupes with
zero held editions). NOT merges: Eusebius/Rufinus/Socrates, Golden Verses vs Hierocles,
1 vs 2 Cor. **Open for Derek:** Hippolytus vs Pseudo-Hippolytus (same *Elenchos*, two
attribution framings).

## Traps hit (memory updated: `project_shwep_reading_room_state.md`)

- **Emit regeneration silently dropped hand-verified chapter deep-links** → now durable in
  `HAND_PAGE_LINKS` (extend, never delete). Same shape as the other session's
  `HAND_REJECTED_HOLDINGS`.
- **The works-held rebuild silently dropped 5 works' holdings**; the standing
  gap-audit+apply-recall pass recovered all 5 on its first run — the "standing, not
  heroic" design paying for itself immediately.
- **Preview alias**: GitHub branch builds can skip commits; the reliable path is a direct
  `npx vercel` from the worktree + alias + verify on the ALIAS host (host-scoped markers
  never show on the bare deployment URL). Never `| tail` a vercel deploy.
- Optional TS field computed but never attached passes tsc silently — curl the served
  flight data to verify a render, not the types.

## State

Preview live + verified: https://sourcelibrary-episode-sources.vercel.app (bare domain
307s → /shwep since a1ac1770). Works DB: 463 works / 325 held / 138 acquire; 212 episodes
with reader links. Both issues carry closing status comments.

## Next

- Derek: Hippolytus framing call; reply to Earl (letter still unsent — see companion
  handoff); the ~98 cross-assignments are mostly the multi-work-volume modeling limit
  (#3708 volume field is the real fix).
- Next refresh (`--all`) exercises every new stage automatically, incl. section-aware
  extraction.
