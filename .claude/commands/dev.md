Set up the development environment using tmux.

Run `dev-sl` to create the tmux workspace, or if already running, show the session status with `tmux list-sessions` and `tmux list-panes -t sourcelibrary`.

If the dev server isn't running, start it in the appropriate pane.
