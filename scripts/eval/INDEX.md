# scripts/eval — index

<!-- GENERATED by scripts/eval/build-index.mjs. Do not hand-edit: fix the
     script's docstring and re-run `node scripts/eval/build-index.mjs`. -->

116 scripts. Before writing a new one, look here and in
`PREREGISTRATION-*.md`; the prior-art guard will ask you what you checked.

## Reusable library — `scripts/eval/lib/`

**Check this first.** These are the pieces that have already been factored out;
re-implementing one is the most common way work gets duplicated here.

| module | exports |
|---|---|
| `lib/embedding-eval.mjs` | `embedTexts`, `embedText`, `evaluatePage`, `evaluateCorpus`, `evaluateRunConsistency` |
| `lib/metrics.mjs` | `tokenize`, `levenshtein`, `charSimilarity`, `cer`, `syllableSimilarity`, `mcr`, `bleu4`, `rougeL`, `cosineSimilarity`, `cosineDistance`, `cleanText`, `pairwiseMetrics`, `stripWrappers`, `normalizeCJK`, `subsequenceCER`, `SCRIPT_DEFS`, `normalizeForScript`, `subsequenceWER`, `greedySpanStats`, `windowedErrorRate`, `GUARD_THRESHOLDS`, `scoreAgainstReference`, `deEntity`, `toAgreementWords`, `agreementWords`, `scriptClassOf`, `toAgreementChars`, `agreementChars`, `agreementPrimary` |
| `lib/paired-stats.mjs` | `resetSeed`, `mean`, `binomTwoSided`, `bootstrapCI`, `diffCI` |
| `lib/production-prompt.mjs` | `getProductionOcrPrompt`, `withIntervention` |
| `lib/report.mjs` | `saveResults`, `loadLatestResults`, `listResults`, `generateConsistencyReport`, `generateEmbeddingReport`, `generateMatrixReport`, `saveBlogPost` |
| `lib/revision-source.mjs` | `READING_SOURCES`, `MAINTENANCE_RE`, `isMaintenanceSource`, `rowIsMaintenance` |
| `lib/runners.mjs` | `estimateCost`, `runGemini`, `runClaude`, `runMistralOcr`, `runMistralChat`, `isMuleModel`, `runMuleRouter`, `isScalewayModel`, `runScaleway`, `isReplicateOcrModel`, `runReplicateDeepSeekOcr`, `resolveModel`, `isClaudeModel`, `isMistralOcrModel`, `isMistralModel`, `runModel`, `fetchImage` |
| `lib/sampling.mjs` | `loadEnv`, `connect`, `disconnect`, `loadCorpusRegistry`, `getCorpus`, `samplePages`, `getPage` |
| `lib/wikisource-text.mjs` | `cleanPageText`, `pageQuality` |

## Preregistrations

Experiments with hypotheses and decision rules fixed **before** the paid run.
Read these for the house conventions before designing a new study.

- `PREREGISTRATION-bench2-print.md`
- `PREREGISTRATION-prompt-ablation.md`
- `PREREGISTRATION-repeat-instability.md`

## Scripts

| script | what it does |
|---|---|
| `bench2-coverage-report.mjs` | bench2-coverage-report.mjs — quality AND coverage, per engine per language. |
| `bench2-escalation-report.mjs` | bench2-escalation-report.mjs — decision rule 3 of PREREGISTRATION-bench2-print.md. |
| `bench2-export.mjs` | bench2-export.mjs — export the pinned ground-truth pages as image files + a manifest, for running self-hosted OCR engines (CHURRO, Surya, Kraken, BDRC…) on a remote box. |
| `bench2-run-model.mjs` | bench2-run-model.mjs — run an API model over a bench2-export.mjs image directory. |
| `bestedition-shwep-eval.mjs` | Eval gate for the #3888 bestEdition ranking change. |
| `blank-page-study.mjs` | Does the model transcribe a page that has nothing on it? (#3444 Tier 2) THE CLEANEST REFERENCE-FREE TEST WE HAVE. |
| `build-ctext-groundtruth.mjs` | Build ctext-sourced OCR ground-truth files for the Chinese corpus. |
| `build-gold-annotator.mjs` | build-gold-annotator.mjs — first-translation HUMAN gold-standard system (sampler + annotator). |
| `build-index.mjs` | mjs ranks experiments still WORTH RUNNING; it does not describe what already exists. |
| `build-observations.mjs` | Build the append-only OCR-eval OBSERVATIONS dataset (#3212 follow-up). |
| `build-reference-groundtruth.mjs` | Build reference-based OCR ground truth for any language (#3212). |
| `build-review-annotator.mjs` | build-review-annotator.mjs — REVIEW/AUDIT-mode annotator for the first-translation gold standard. |
| `build-work-gold-set.mjs` | build-work-gold-set.mjs — generate a stratified human-labeling set of book PAIRS for calibrating the work_id resolver (issue #2909). |
| `calibration-scorecard.mjs` | Calibration scorecard — the reader-first deliverable (#3235). |
| `card-prior-candidates.mjs` | Propose candidate English translations for EMPTY translation cards, from the LoC/ESTC catalogue in `reference_translations`. |
| `cloze-probe.mjs` | Occlusion cloze with SHAPED masks — a graded membership probe (#3235/#3444). |
| `condition-validation.mjs` | Does the `marginalia` condition flag actually detect marginalia? (#3444) The prompt ablation measures a FIRING RATE on the 44 pinned ground-truth pages, which exist for reference-etext coverage. |
| `deposit-ft-dataset.mjs` | deposit-ft-dataset.mjs — deposit an FT dataset snapshot on Zenodo (#3798). |
| `disagreement-classes.mjs` | What do two passes over the SAME leaf actually disagree ABOUT? (#3473) Agreement is one number and it hides distinct failure classes. |
| `disagreement-typology.mjs` | WHAT do two OCR passes disagree ABOUT? A typology of revision disagreement. |
| `embedding-survey.mjs` | Embedding Alignment Survey Properly designed experiment measuring OCR→Translation embedding distance across languages and document types (manuscript vs print). |
| `enrich-verify-warehouse.mjs` | Enrich and verify warehouse books in one pass. |
| `estimate-works-from-chapters.mjs` | estimate-works-from-chapters.mjs — derive a contents-manifest estimate for multi-work container books from the EXISTING `books.chapters[]` index, and validate it by page-range coverage. |
| `export-eval-dataset.mjs` | Export the OCR-eval observations as a self-contained, publishable dataset (#3235). |
| `export-ft-dataset.mjs` | export-ft-dataset.mjs — versioned snapshot of the first-translation verification corpus, for deposit as a citable dataset (#3798). |
| `fabrication-detector.mjs` | py + mss_orderfree.py + mss_report.py — they already implement this agreement/overrun/order-free triad, but in Python, hardcoded to Tibetan folios and Hetzner paths; they cannot be pointed at the p… |
| `ft-alignment-gold-draw.mjs` | Tier-0 ALIGNMENT-GOLD draw — issue #2885(b). |
| `ft-alignment-j1-prompts.mjs` | J1 prompt emitter for the #2885(b) alignment gold. |
| `ft-alignment-judge-auto.mjs` | J0 — cheap deterministic "same work? y/n" metadata oracle for the #2885(b) alignment gold. |
| `ft-alignment-merge-j1.mjs` | Merge J1 verdicts for the #2885(b) alignment gold into one file the scorer reads. |
| `ft-alignment-score.ts` | Scorer for the #2885(b) Tier-0 alignment gold. |
| `ft-catalog-crosscheck.mjs` | First-Translation Catalog Cross-Check Tests whether our verification tool correctly handles books whose authors have known English translations in our translation_catalogs collection. |
| `ft-catalog-match.mjs` | — |
| `ft-constituent-catalog-match.mjs` | ft-constituent-catalog-match.mjs — Tier-0 catalog match for CONTAINER sub-works (#2916 verification, sibling of scripts/eval/ft-catalog-match.mjs). |
| `ft-eval.mjs` | First-Translation Verification Evaluator Subcommands: seed — Query the DB for benchmark candidates, output suggested cases run — Run verification on all benchmark cases, compare to ground truth rep… |
| `ft-gemini-adjudicate.mjs` | Gemini-grounded first-translation adjudicator (issue #2564). |
| `ft-ground-truth.mjs` | First-Translation Ground-Truth Evaluation Builds an external ground-truth dataset by independently querying 5 catalog APIs for English translations of books in our library. |
| `ft-ingest-verdicts.ts` | Ingest Tier-2 adjudicator verdicts into the provenance log (issue #2564). |
| `ft-ladder.ts` | ft-ladder.ts — the FT verification escalation ladder (#3778). |
| `ft-measure.mjs` | Read-only measurement of the first-translation corpus (issue #2564). |
| `ft-pilot-sample.mjs` | Draw one ROUND of the #2880 expanding stratified pilot. |
| `ft-pilot-score-cum.mjs` | #2880 CUMULATIVE scorer (Rounds 1+2) under the CORRECTED not_applicable rubric, plus the #2885a Tier-0 alignment audit (Round 2 has 13 tier0 catalog matches). |
| `ft-pilot-score-r5.ts` | Score a #2880 pilot round and project the corpus estimate. |
| `ft-pilot-score.mjs` | #2880 Round-1 scorer — head-to-head (Tier-1 Gemini vs Tier-2 Claude oracle), per-stratum precision/recall/fabrication, 4-class Cohen's kappa, and the genuine-first rate per stratum with a Wilson 95… |
| `ft-prior-adjudicate.mjs` | ft-prior-adjudicate.mjs — LLM PAIRWISE adjudication of catalog prior-matches (#2916). |
| `ft-quality-harness.ts` | First-Translation QUALITY HARNESS — non-circular eval (issue #2876). |
| `ft-recall-sample.mjs` | Draw a random sample from the NOT-badged first-translation-eligible pool (issue #2564 / #1974 recall question). |
| `ft-reference-set-recall.mjs` | — |
| `ft-reference-set-search.mjs` | ft-reference-set-search.mjs — run every badged book against the reference set and record the SEARCH, not just the answer. |
| `ft-reliability-report.ts` | Verdict-reliability analysis (issue #2564 paper). |
| `ft-render-diff.ts` | ft-render-diff — what does the Tier 3 render swap actually change? (#3726) READ-ONLY. |
| `ft-rung3-queue.ts` | ft-rung3-queue.ts — build the rung-3 (Claude ft-verify) worklist from the LEDGER, not from per-run report files (#3881 pass 2). |
| `ft-score-groundtruth.mjs` | Score Gemini adjudications against the 33-case ground-truth set (#2564). |
| `ft-screening-triage.mjs` | ft-screening-triage.mjs — the SCREENING judgements, recorded as code. |
| `ft-search-unexamined.mjs` | ft-search-unexamined.mjs — grounded, LOGGED web search for prior translations of FT-badged books that have NO documented search yet (#2916 evidence floor). |
| `ft-stratified-sample.ts` | Stratified sampler for the first-translation corpus estimate (issue #2564). |
| `ft-structural-prefilter.mjs` | Stage A — structural pre-filter for the first-translation census (#2780). |
| `ft-tier2-report.ts` | Turn bulk Tier-2 verdicts into the corpus estimate (issue #2564). |
| `ft-translation-queue.mjs` | ft-translation-queue.mjs — what has nobody translated yet? (#3459) The reference set was built to defend claims we already make. |
| `ft-verify-demote-packet.mjs` | ft-verify-demote-packet.mjs — the sign-off packet for proposed demotes. |
| `ft-verify-gate.mjs` | ft-verify-gate.mjs — Stage-2 verification gate for first-translation verdicts (#2564). |
| `ft-work-registry-pilot.mjs` | ft-work-registry-pilot.mjs — the work-grain pilot (#3881 north star, phase 1 in miniature). |
| `gen-ft-groundtruth.mjs` | Generates the bulk Tier-2 adjudication WORKFLOW script with the 462-book worklist embedded (issue #2564). |
| `gen-ft-recall.mjs` | Generates the bulk Tier-2 adjudication WORKFLOW script with the 462-book worklist embedded (issue #2564). |
| `gen-ft-reliability.mjs` | Generates the bulk Tier-2 adjudication WORKFLOW script with the 462-book worklist embedded (issue #2564). |
| `gen-ft-tier2-rerun.mjs` | Generates the bulk Tier-2 adjudication WORKFLOW script with the 462-book worklist embedded (issue #2564). |
| `gen-ft-tier2-workflow.mjs` | Generates the bulk Tier-2 adjudication WORKFLOW script with the 462-book worklist embedded (issue #2564). |
| `gloss-demo-server.mjs` | "Touch the Greek" — local prototype of the reader gloss (issues #4320/#4332). |
| `hard-page-census.mjs` | Census of hard-page candidates, by failure class (#3469). |
| `hard-page-sample.mjs` | Draw a labelled exemplar set of HARD pages, one bucket per failure class (#3469). |
| `harvest-wikisource-gt.mjs` | harvest-wikisource-gt.mjs — bulk OCR ground truth from Wikisource proofread pages. |
| `ia-ocr-baseline.mjs` | IA-OCR corpus baseline (#3235 — "IA-OCR corpus baseline" entry in .claude/docs/ocr-memorization-paper.md, priority 2 after the calibration scorecard). |
| `identify-bench.mjs` | Identify benchmark (#3193): measures "photo of an artwork -> the right library image" retrieval quality. |
| `lexicon-grc-treebank-eval.mjs` | Validate lexicon_lemma_map_grc against the UD Ancient Greek (Perseus) treebank: for each human-verified (form, lemma) token, if the form is in our map, does our key set contain the gold lemma's LSJ… |
| `lexicon-la-treebank-eval.ts` | Validate the Latin lemma machinery against the UD Latin-ITTB treebank (Index Thomisticus: Aquinas — medieval Latin, the closest gold standard to our early modern corpus). |
| `lexicon-lookup-eval.ts` | Phase-1 eval gate for the parsing reader (#3823): measure the lexicon lookup chain against REAL OCR — not clean critical editions — before any UI ships on top of it. |
| `merge-claude-adjudications.mjs` | merge-claude-adjudications.mjs — join Claude Tier-2 adjudication verdicts with the frozen gold-standard sample into a score-ready label file. |
| `neighbour-leaf-test.mjs` | Did this revision's text come from the page it sits on, or from a NEIGHBOUR? The problem this solves: the printed `<page-num>` identifies which leaf a pass read, but it is absent on 39% of ocr rows… |
| `ocr-difficulty-taxonomy.mjs` | Build an OCR-difficulty taxonomy from the corpus, WITH concrete examples. |
| `ocr-prompt-v17-acceptance.mjs` | Acceptance run for OCR prompt v17 (#4195 checklist + #4584). |
| `ocr-purity-probe.mjs` | OCR residue purity probe — detect UNTAGGED AI text serving as "original." Purpose: the quote / licensing / search surfaces serve the OCR residue (stripEditorialWrappers + generic tag strip) as the … |
| `ocr-triage.mjs` | Rank pages for human spot-checking, and emit a worked example set. |
| `page-verification-sample.mjs` | — |
| `plate-flip-rate.mjs` | PLATE-FLIP RATE — a baseline metric for a prompt gap, measurable for free. |
| `plato-dialogue-server.mjs` | Dialogue with Plato — local prototype (the wish behind issue #4320). |
| `prompt-ab.mjs` | — |
| `prompt-ablation.mjs` | Prompt ablation: does asking a VLM for structured metadata alongside the transcription degrade the transcription — and do required fields actually fire? Motivation (#3444). |
| `qa-eval.mjs` | QA-Eval: Scalable OCR & Translation Quality Evaluation Framework Subcommands: consistency — Run OCR N times per model, compute MCR and pairwise similarity cross-model — Compare best runs across mod… |
| `recommend-experiments.mjs` | Experiment recommender (#3235): turn OBSERVATIONAL signals into a prioritized queue of PAID experimental runs, each with a hypothesis, a design, and a cost estimate. |
| `reference-error-rate.mjs` | mjs scores an ENGINE against a reference, and lib/metrics.mjs supplies the metric — both are reused here unchanged so the number lands on the same scale as the bench's accuracy figures. |
| `refresh-ws-references.mjs` | refresh-ws-references.mjs — re-clean the pinned Wikisource references from source. |
| `reocr-pairing-check.mjs` | Is a page's stored OCR text actually a reading of the image now sitting under it? (#3473) Every instrument tried so far answers this indirectly and badly: - the printed `<page-num>` is transcribed … |
| `repeat-instability-draw.mjs` | Draw a BLINDED, within-book matched sample for the repeat-instability study. |
| `report-ablation.mjs` | Analysis for the prompt ablation (#3444). |
| `report-arms.mjs` | Experiment-arm analysis for the scorecard's tagged runs (#3235). |
| `report-canonical-gap.mjs` | Canonical vs non-canonical gap analysis over the observations dataset (#3235). |
| `report-occlusion.mjs` | Occlusion/degradation pilot report (#3235 — "Occlusion cloze pilot" in .claude/docs/ocr-memorization-paper.md). |
| `report-tier1-attribution.mjs` | Read the Tier-1 attribution checkpoint file and report (#3444). |
| `reverify-batch.mjs` | Batch re-verify first-translation books with the new 7-tool pipeline. |
| `reverify-flagged.mjs` | Re-verify the 77 flagged cases from the catalog cross-check using the new 7-tool pipeline. |
| `revision-agreement-annotation-probe.mjs` | How much of measured OCR revision DISAGREEMENT is annotation, not text? (#3235) `stripWrappers()` removes the editorial BLOCK wrappers (<meta>, <summary>, …) content-and-all, but for inline marks (… |
| `revision-agreement-corpus.mjs` | Corpus-scale OCR revision-agreement analysis (#3235 double-OCR workstream). |
| `revision-agreement-exposure.mjs` | Does agreement rise with BIBLIOGRAPHIC EXPOSURE? (#3235) Applies the prior half of the /blog/did-the-ai-read-this method — "a work with no English translation cannot have been encountered in Englis… |
| `revision-agreement-pilot.mjs` | Agreement→accuracy calibration pilot (#3235 double-OCR workstream). |
| `score-gold-standard.mjs` | score-gold-standard.mjs — aggregate human gold-standard labels into the paper/eval numbers. |
| `score-review-standard.mjs` | score-review-standard.mjs — aggregate a REVIEW/AUDIT export from ft-review-annotator.html. |
| `score-transcripts.mjs` | Score a directory of transcriptions of the pinned ground-truth pages against the published references — same two-stage scoring as the scorecard's stored-OCR column. |
| `score-work-gold-set.mjs` | score-work-gold-set.mjs — score the labeled work-identity gold set. |
| `search-quality-eval.mjs` | Search Quality Evaluation Suite Tests search across three consumer profiles: 1. |
| `stats-cross-model.mjs` | Paired statistics for cross-model OCR comparisons on the pinned reference set (#3235). |
| `tesseract-baseline.mjs` | Non-generative OCR baseline → difference-in-differences subsidy (#3235). |
| `tier1-attribution.mjs` | Does the Tier-1 prompt change reduce UNATTRIBUTED AI prose? (#3444) Tier 1 is two edits to the OCR prompt, no new tags and no data migration: regroup the annotation list by PROVENANCE ("from the pa… |
| `true-repeat-count.mjs` | How many TRUE REPEATS does a corpus JSONL contain, and what does excluding bulk-maintenance rows do to that count? (#3473) The headline "63,572 true repeats" is not a field in the corpus summary — … |
| `tuned-vs-base-eval.mjs` | Held-out eval: tuned sl-greek-translator-v1 vs base gemini-2.5-flash-lite on the whole-book validation split (issue #4320). |
| `wikisource-gt-to-groundtruth.mjs` | wikisource-gt-to-groundtruth.mjs — convert harvest-wikisource-gt.mjs output into ground-truth files the existing scorers read. |
| `work-resolver-probe.mjs` | work-resolver-probe.mjs — measure how well the work_id resolver clusters books into works, WITHOUT a gold set. |
