// Spanish twin of the reader's loading shell. The chrome it mocks up (bars,
// pane frames) carries no text, so it is locale-neutral — pure re-export.
export { default } from '@/app/book/[id]/page/[pageId]/(reader)/loading';
