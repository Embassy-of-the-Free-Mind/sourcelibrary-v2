// The mycology gallery masonry is now the shared GalleryMasonry.
// Kept as a thin re-export so the mycology page's import path is unchanged.
export { default, type Plate } from '@/components/GalleryMasonry';
