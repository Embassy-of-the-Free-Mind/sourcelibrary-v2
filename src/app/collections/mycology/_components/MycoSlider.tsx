// The mycology "First translations" slider is now the shared BookSlider.
// Kept as a thin re-export so the mycology page's import path is unchanged.
export { default, type MiniBook } from '@/components/BookSlider';
